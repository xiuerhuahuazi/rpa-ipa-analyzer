# Report Template — MUST Follow This Exact Structure

Every analysis report must include ALL sections below. Do not skip sections.

---

# {项目名称} — IPA Studio RPA 业务与代码分析报告

> 项目版本：{version} | 设计器：{designer_version} | 执行器：{executor_version} | 分析日期：{date} | 项目类型：{data_processing / web_automation / mixed}

---

## 一、整体工作流分析

### 1.1 项目架构概览

**必须包含**：
- 项目类型判定及依据（从 processResult.json 组件分布判定）
- 分层架构 Mermaid 图（主流程→业务子流程→COMMON 公用子流程）
- 各层职责一句话说明

### 1.2 主流程完整路径

**必须包含**：文本流程图，标注每个节点的：
- 序号、类型（Python/JS/子流程/条件/循环/异常）
- 作用描述
- 输入/输出变量
- 关键决策点及其条件

格式：
```
[1] 流程开始
  ├─ [2] 打印业务日志: "流程开始"
  ├─ [3] [Python] 日志类初始化
  │     输入: LOG_NAME, 输出: out_logger, log_path
  ├─ [4] [子流程] 创建工作目录
  │     输入: in_workpath → 输出: out_workpath
  ├─ [5] [异常处理] try/catch/finally
  │     ├── TRY → [6] 子流程: 业务流程.json
  │     ├── CATCH → [7] 再次抛出异常
  │     └── FINALLY → [8] 上传结果 → [9] 上传日志 → [10] 删除临时文件
  └─ [11] 流程结束
```

### 1.3 条件分支与路由逻辑

**必须包含**：所有 process_if 节点的条件和分支走向，说明参数如何驱动流程分支。

### 1.4 子流程调用关系

**必须包含**：表格列出所有子流程 — 文件路径、节点数、调用方、传入/传出变量。

---

## 二、节点级详细拆解（核心）

### 2.1 阶段划分与概述

**必须包含**：将整个流程按业务逻辑划分为阶段（如：数据加载→数据清洗→合并匹配→计算→导出），每个阶段说明：目的、涉及节点数、输入数据、输出数据、关键决策。

### 2.2 代码节点逐一详解

**对每个 script_python_execute 和 browser_inject_js_code 节点，逐一分析**：

```
#### 节点 N{n}: {show_name} (`{node_id}`)

| 属性 | 值 |
|------|-----|
| 类型 | {component_id} |
| 作用 | {一句话业务目的} |
| 输入变量 | {变量名} ← {来源} (全局参数/上游节点/硬编码) |
| 输出变量 | {变量名} → {下游节点} |
| 关联Excel | {Resources目录中的文件名} (读/写) |

**代码功能概述**：{2-3句话说明这段代码做什么}

**关键数据结构**：
- {变量名}: {类型}, {用途}

**处理步骤**：
1. {步骤1说明}
2. {步骤2说明}

**核心业务规则**：{代码中体现的计算公式、筛选条件、匹配逻辑}

**异常处理与边界情况**：{代码中的try/except、空值检查、边界条件}

**代码片段**（关键部分）：

\`\`\`python
# {中文注释}
{核心逻辑代码}
\`\`\`
```

### 2.3 UI 自动化节点分析（Web 自动化项目必须）

**必须包含**：
- 页面流转图（URL1 → URL2 → URL3）
- 关键 UI 元素清单（选择器、操作类型、用途）
- JavaScript 注入脚本分类（DOM 操作类/数据提取类/状态检测类/值注入类）
- 用户交互序列描述

### 2.4 完整流程序列

**必须包含**：按边顺序列出所有节点的执行顺序，标注分支和循环边界。

---

## 三、全局参数与配置分析

### 3.1 参数完整列表

**必须包含**：

| # | 参数Key | 类型 | 描述 | 默认值 | 使用位置（节点/子流程） |
|---|---------|------|------|--------|------------------------|
| 1 | ... | ... | ... | ... | ... |

### 3.2 参数-流程映射

**必须包含**：说明每个参数在哪个节点的哪个变量中被引用，以及如何影响流程行为（如 TARGETS 参数驱动条件分支）。

### 3.3 双环境配置差异（如适用）

对比 globalParams.json（生产）与 globalParamsAll.json（模板）的差异。

---

## 四、业务逻辑深度解读

### 4.1 整体业务目标

**必须包含**：
- 这个 RPA 解决什么业务问题
- 在业务流程中的定位（日报/月报/一次性操作）
- 输入什么、产出什么

### 4.2 核心业务概念

**必须包含**：涉及的专业术语解释表。

| 术语 | 英文/缩写 | 业务含义 |
|------|----------|----------|
| ... | ... | ... |

### 4.3 数据流转全路径

**必须包含**：从 Resources 输入文件 → 处理过程 → output_file 输出的完整端到端 ASCII 流程图，标注每个环节的文件名和处理内容。

```merm
Resources/                     处理阶段                    output_file/
├─ 文件1.xlsx ─────┐
├─ 文件2.xlsx ───┐ │       ┌──────────────┐
│                │ ├──→ [Python]数据清洗  │
│                │ │       └──────┬───────┘
│                │ └─────────────┘
│                │         ┌──────▼───────┐
│                └────→ [Python]多表合并   │
│                          └──────┬───────┘
│                                 │
│                          ┌──────▼───────┐
│                          │ [导出]最终文件 │──→ 结果文件.xlsx
│                          └──────────────┘
```

### 4.4 核心业务规则与计算公式

**必须包含**：
- 数据匹配规则（JOIN 键、匹配优先级）
- 筛选与排除规则（包含实际的条件表达式）
- 核心计算公式（含数学公式和代码实现）
- 业务判断逻辑（如"推荐操作"的条件树）

### 4.5 Excel 读写完整映射

**必须包含**：

| 操作类型 | 节点 | Excel文件名 | Sheet名 | 读取列/写入内容 |
|----------|------|------------|---------|----------------|
| 读取 | N3 | 合同台账.xlsx | 合同台账查询结果 | 全部列 |
| 读取 | N3 | 合同台账.xlsx | 采购订单 | 全部列 |
| 写入 | N29 | 财务报表_含公式.xlsx | — | 55列含公式 |

---

## 五、综合分析

### 5.1 组件使用统计与洞察

**必须包含**：
- 从 processResult.json 提取的完整使用统计表
- 对统计数据的解读（为什么某种组件使用最多、反映了什么设计模式）

### 5.2 优缺点分析

**必须包含**：

| 优点 | 说明 |
|------|------|
| 1. ... | ... |

| 缺点 | 严重程度 | 说明 |
|------|----------|------|
| 1. ... | 高/中/低 | ... |

### 5.3 优化建议

**必须包含**，按类别分列：

| 类别 | 建议 | 优先级 |
|------|------|--------|
| 代码结构 | ... | 高/中/低 |
| 性能 | ... | 高/中/低 |
| 维护性 | ... | 高/中/低 |
| 可读性 | ... | 高/中/低 |
| 异常处理 | ... | 高/中/低 |
| 日志记录 | ... | 高/中/低 |

### 5.4 潜在风险点

**必须包含**：

| 风险类型 | 严重程度 | 触发条件 | 影响范围 | 建议措施 |
|----------|----------|----------|----------|----------|
| ... | 高/中/低 | ... | ... | ... |

---

## 附录

### A. 项目文件清单

**流程文件表**：文件名、大小、节点数、代码节点数

**配置文件表**：文件名、用途

**资源文件表**：文件名、大小、用途

### B. 节点索引

**必须包含**：全部节点的索引表 — 序号、节点ID、类型、名称、所在文件
