# {项目名称} — IPA Studio RPA 分析报告

> 项目版本：{version} | 设计器：{designer_version} | 执行器：{executor_version} | 分析日期：{date} | 项目类型：{data_processing / web_automation / mixed} | 分析深度：{quick / standard / deep}

---

## 报告章节 ↔ 分析层映射

| 报告章节 | 数据来源（分析层） |
|----------|-------------------|
| §1 整体工作流分析 | 层 1.1-1.2（项目建模） |
| §2 节点级详细拆解 | 层 2.1-2.2（代码+序列分析） |
| §3 全局参数与配置 | 层 1.1（元数据发现） |
| §4 业务逻辑解读 + 设计模式 | 层 2.3（业务解读+模式匹配） |
| §5 综合分析 + 优化建议 | 层 2.3（模式评估）+ 层 3.1（报告） |
| §6 自动化审计 | 层 3.3（并行审计，仅 deep 模式） |
| 附录 A/B | 层 1.1-1.2（项目建模） |

---

## 一、整体工作流分析

### 1.1 项目架构概览
项目类型判定依据(从 processResult.json) + 分层架构 Mermaid 图 + 各层职责。

### 1.2 主流程完整路径
文本流程图，每节点标注序号/类型/作用/输入输出/决策点。
```
[1] 流程开始 → [2] 打印日志 → [3] [子流程]创建工作目录 → [4] [异常处理]try/catch/finally
  ├── TRY → [5] 业务子流程 → [6] 清理临时文件
  ├── CATCH → [7] 记录错误 → [8] 再次抛出
  └── FINALLY → [9] 打印结束
```

### 1.3 条件分支与路由逻辑
所有 `process_if` 节点的条件和分支走向。

### 1.4 子流程调用关系
| 子流程文件 | 节点数 | 调用方 | 传入变量 | 传出变量 |
|-----------|--------|--------|----------|----------|

---

## 二、节点级详细拆解

> **Quick 模式**: 仅输出 §2.1 阶段划分和 §2.4 主流程顶层序列，跳过 §2.2 和 §2.3。

### 2.1 阶段划分
按业务逻辑分阶段（加载→清洗→合并→计算→导出），每阶段：目的/节点数/输入/输出/关键决策。

### 2.2 代码节点逐一详解
对每个 `script_python_execute` 和 `browser_inject_js_code` 节点：

```
#### 节点 N{n}: {show_name} (`{node_id}`)

| 属性 | 值 |
|------|-----|
| 类型 | {component_id} |
| 作用 | 一句话业务目的 |
| 设计模式 | [UP-0X 模式名]（若匹配，标注置信度：高/中） |
| 输入变量 | {变量名} ← {来源} |
| 输出变量 | {变量名} → {下游} |
| 关联Excel | {文件名} (读/写) |
| 代码文件 | `.extracted_nodes/{file}` |
| code_hash | `{hash前12位}` |

**代码功能概述**：2-3句
**关键数据结构**：变量名: 类型, 用途
**处理步骤**：1. ... 2. ...
**核心业务规则**：计算公式/筛选条件/匹配逻辑
**异常处理与边界情况**

> **Token 规则（3.1+）**：禁止粘贴完整源码。若需说明算法，最多引用 ≤15 行关键片段；默认只写路径引用。
```

骨架中的「§2.2 代码节点索引」表由 `generate_skeleton.py` 生成，详解在索引之后按需展开。
### 2.3 UI 自动化节点分析（仅 Web 自动化项目，数据处理项目跳过）
页面流转图 + UI 元素清单 + JS 注入分类 + 交互序列。

### 2.4 完整流程序列
按边顺序列出全部节点执行序列，标注分支/循环边界。Quick 模式：仅主流程顶层。

---

## 三、全局参数与配置分析

### 3.1 参数完整列表
| # | 参数Key | 类型 | 描述 | 默认值 | 使用位置 |
|---|---------|------|------|--------|----------|

### 3.2 参数-流程映射
每个参数在哪个节点被引用，如何影响流程行为。

### 3.3 双环境配置差异
globalParams.json（生产）vs globalParamsAll.json（模板）。

---

## 四、业务逻辑深度解读

> **Quick 模式**: 仅 §4.1 一句话推测业务目标，其余跳过。

### 4.1 整体业务目标
解决什么问题、定位（日/月/一次性）、输入→输出。

### 4.2 核心业务概念
| 术语 | 英文/缩写 | 业务含义 |
|------|----------|----------|

### 4.3 数据流转全路径
端到端 ASCII 流程图：Resources/ → 处理节点 → output/，标注文件名和处理内容。

### 4.4 核心业务规则与计算公式
JOIN键+优先级、筛选条件(含表达式)、计算公式(数学+代码)、决策树。

### 4.5 Excel 读写映射
| 操作 | 节点 | 文件名 | Sheet | 列/内容 |
|------|------|--------|-------|---------|

Resources 文件清单：文件名、大小、用途。

---

## 五、综合分析

### 5.1 组件使用统计与设计模式

**组件统计表**（来自 processResult.json，按使用量降序排列）。

**识别到的设计模式**（若层 2.3 有匹配）：
| 模式 | 匹配节点 | 置信度 | 说明 |
|------|----------|--------|------|

**未命中模式**：列出 `patterns_universal.md` 中未命中的通用模式及原因。

### 5.1.x 启发式提取组件（未在 ipa_format.md 中定义的组件）
| # | 组件ID | 出现次数 | 提取方法 | 发现字段 |
|---|--------|----------|----------|----------|
| 1 | `{component_id}` | {count} | 启发式 | {field_1}, {field_2}, … |

这些组件的字段提取未经人工语义验证，后续使用次数多了可升级为已知类型。

### 5.2 优缺点分析
优点表 + 缺点表（含严重程度）。

### 5.3 优化建议
| 类别 | 建议 | 优先级 |
|------|------|--------|
代码结构/性能/维护性/可读性/异常处理/日志记录。对识别到的设计模式评价适用性及是否可替换。

### 5.4 潜在风险点
| 风险类型 | 严重程度 | 触发条件 | 影响范围 | 建议措施 |
|----------|----------|----------|----------|----------|

---

## 六、自动化审计摘要（仅 deep 模式）

> 完整审计报告见 `AUDIT_REPORT.md`。

| 审计维度 | 严重 | 高 | 中 | 低 | 合计 |
|----------|------|-----|-----|-----|------|
| 安全性 | {N} | {N} | {N} | {N} | {N} |
| 性能 | {N} | {N} | {N} | {N} | {N} |
| API 契约 | {N} | {N} | {N} | {N} | {N} |
| 错误处理 | {N} | {N} | {N} | {N} | {N} |
| 测试缺口 | {N} | {N} | {N} | {N} | {N} |
| 文档漂移 | {N} | {N} | {N} | {N} | {N} |
| **合计** | **{N}** | **{N}** | **{N}** | **{N}** | **{N}** |

**审计健康评估**：{1-2句话总结}

**Top 5 关键发现**：
1. {severity} — {finding_summary}（{file}:{line_range}）
...
5. ...

---

## 附录

### A. 项目文件清单
流程文件表（文件名/大小/节点数/代码节点数）+ 配置文件表 + 资源文件表。

### B. 节点索引
全部节点：序号、节点ID、类型、名称、所在文件。Quick 模式仅含代码节点。
